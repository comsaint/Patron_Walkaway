# Deploy: run pip install -r requirements.txt from repo root (so -e .. resolves)
# Phase 2 P0-P1: mlflow for export_predictions_to_mlflow when run on deploy machine (or same box as scorer).
Flask==3.1.3
mlflow==3.10.1
pandas==2.3.3
numpy==2.4.2
joblib==1.5.3
lightgbm==4.6.0
PyYAML==6.0.3
python-dotenv==1.2.1
clickhouse-connect==0.13.0
numba==0.64.0
llvmlite==0.46.0
pyarrow==23.0.1

# Application package (walkaway_ml). From repo root: -e . installs this repo as walkaway_ml
-e .
